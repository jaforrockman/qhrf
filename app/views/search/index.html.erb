<!DOCTYPE html>
<html>
<head>
	<title>Search</title>
</head>
<body style="background-color: #b5f7bf; " >

<div class="sidenav2">
   
       <h2 align="center">Tag navigation</h2>
     
</div>

<div class="main2">
<div class="body-style">

	<div align="center">  
	<%= form_tag(search_path, method: "get") do %>
	   	<div><%= text_field_tag :query, nil, placeholder: "Search for any topic" , class: "form-control2" %>
	   	<button class="button button1"> <%= submit_tag "submit", style: "display: none;", class: "btn btn-primary" %> Search</button></div>
 	 <% end %>
 	 </div>
 	 
	</br>

	
	<div  align="center">
				 
		<%@ayats.order("ayat_number ASC").each do |ayate|%>
			<div class="content" > 
				<div class="ayat"><span class="ayat_ref">Book:<%=Book.find(ayate.book_id).name %> Surah/Chapter:<%=Surah.find(ayate.surah_id).surah_name%>/Ayat No:(<%= ayate.ayat_number%>)</span><hr>
				<%= ayate.ayat_text %></div>

			</div>
		</br>
		<%end%>
		
	 </div>	
</div>	
</div>
</body>
